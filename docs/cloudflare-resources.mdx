---
title: Cloudflare resources
description: Know which Cloudflare resources each Flary runtime feature needs.
section: Start
order: 2
---

The complete Flary runtime is Cloudflare-native today. A production deployment
needs a Cloudflare account. The account can be yours or the host
application's account.

## Resource matrix

| Feature | Worker | Durable Objects | D1 | R2 |
| --- | --- | --- | --- | --- |
| Direct function call | No | No | No | No |
| Function HTTP route | Yes | No | No | No |
| Compile `*.prompt.md` | No | No | No | No |
| Function-first durable chat or support thread | Yes | Yes (SQLite) | Generated catalog | Generated archive |
| Persistent coding agent with small files | Yes | Yes (SQLite) | Generated catalog | Generated archive |
| Low-level D1 run adapter | Yes | Optional | Yes | No |
| Files larger than 1,500,000 bytes | Yes | Yes (SQLite) | No | Yes |
| R2 history fallback | Yes | Yes (SQLite) | No | Yes |

Cloudflare Sandbox, Dynamic Workers, and Artifacts are optional integrations.
They are not required for a basic durable agent.

## What each resource stores

### Durable Object

Flue owns the canonical thread transcript and execution stream in the thread
Durable Object. The generated Thread Control Durable Object keeps the
rebuildable session ledger, approvals, audit indexes, subagent mailboxes,
goals, rollback markers, process state, and read cursors. It does not replace
the Flue transcript.

### D1

Persistent agents use D1 for tenant-scoped thread list indexes. D1 is not
the canonical transcript or the source of thread execution state.

The advanced `D1FlaryRunRepository` adapter stores:

- Trusted run and tenant bindings.
- Flue admission receipts.
- Normalized event projections for replay.
- Materialized run results.
- Idempotency keys.

D1 does not copy Flue's canonical transcript.

Apply `FLARY_RUNS_D1_MIGRATION` through your application's migration process.

### R2

The workspace runtime keeps files up to `1,500,000` bytes in workspace
SQLite. Larger files go to R2. R2 can also store immutable history when the
Cloudflare Artifacts binding is not available. Thread Control automatically
compresses, encrypts, and seals records outside its 10,000-record hot window
to R2. It verifies each encrypted segment before a read.

Use one private bucket for each environment. Flary derives tenant-scoped
object keys, but the host must still authorize every read, write, delete, and
signed transfer request.

## Local development

Wrangler can emulate a Worker, Durable Objects, and D1 on the local computer.
You can build and test before you create production resources. R2 behavior can
also be tested locally, but production large-file storage still needs a real
R2 binding.

## Deployment outline

1. Sign in with `wrangler login`.
2. Use the Durable Object, D1, R2, and Queue bindings generated by Flary Vite.
   Wrangler 4.45 or later can provision D1 and R2 from binding-only
   configuration.
3. Create the generated projection and dead-letter queues before the first
   production deployment.
4. Set `FLARY_SESSION_ARCHIVE_KEY` to a random secret of at least 32
   characters.
5. Apply `FLARY_RUNS_D1_MIGRATION` only when you also use the advanced
   low-level run adapter.
6. Store provider keys or OAuth credentials as encrypted host data.
7. Deploy the Worker.

Flary does not force Better Auth, Clerk, Auth0, or another identity system.
The host authenticates the request and returns a trusted
`TrustedRunContext`.
