diff --git a/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs b/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs
index 94032552cc83d3596ae6e53bd46ec6c9c9749b01..25f029d609318614d664c6fe790cb576c1799bef 100644
--- a/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs
+++ b/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs
@@ -550,6 +550,7 @@ var Session = class {
 	activeTimeoutAt;
 	activeSubmissionId;
 	activeSubmissionAttemptId;
+	activeCacheRetention;
 	conversationWriter;
 	attachmentStore;
 	canonicalAssistant;
@@ -565,7 +566,11 @@ var Session = class {
 		if (this.activeTurnId === void 0) this.activeTurnId = generateTurnId();
 		const turnId = this.activeTurnId;
 		const operationId = this.activeOperationId ?? generateOperationId();
-		this.emitTurnRequest(turnId, "agent", model, context, options);
+		const streamOptions = {
+			...options,
+			...this.activeCacheRetention ? { cacheRetention: this.activeCacheRetention } : {}
+		};
+		this.emitTurnRequest(turnId, "agent", model, context, streamOptions);
 		const operation = {
 			type: "model",
 			turnId
@@ -575,7 +580,7 @@ var Session = class {
 			operationId,
 			turnId
 		});
-		return interceptExecution(operation, executionContext, async () => wrapProviderStream(streamSimple(model, context, options), operation, executionContext));
+		return interceptExecution(operation, executionContext, async () => wrapProviderStream(streamSimple(model, context, streamOptions), operation, executionContext));
 	};
 	canonicalEnvelope(type, id = generateConversationRecordId()) {
 		return {
@@ -1881,9 +1886,11 @@ var Session = class {
 		const previousTools = this.agentLoop.state.tools;
 		const previousModel = this.agentLoop.state.model;
 		const previousThinkingLevel = this.agentLoop.state.thinkingLevel;
+		const previousCacheRetention = this.activeCacheRetention;
 		const resolvedModel = this.resolveModelForCall(options.model);
 		this.agentLoop.state.model = resolvedModel;
 		this.agentLoop.state.thinkingLevel = this.resolveThinkingLevelForCall(options.thinkingLevel);
+		this.activeCacheRetention = options.cacheRetention;
 		const builtinToolGroups = this.createBuiltinToolGroups(this.env, options.tools, options.model, options.thinkingLevel, options.activePackagedSkills);
 		this.agentLoop.state.tools = this.assembleModelTools(builtinToolGroups, [...this.agentTools, ...options.tools], options.extraTools ?? []);
 		try {
@@ -1894,6 +1901,7 @@ var Session = class {
 			this.agentLoop.state.tools = previousTools;
 			this.agentLoop.state.model = previousModel;
 			this.agentLoop.state.thinkingLevel = previousThinkingLevel;
+			this.activeCacheRetention = previousCacheRetention;
 		}
 	}
 	resolveDeclaredSubagent(name) {
@@ -2775,6 +2775,9 @@ var Session = class {
 			submissionAttempt: options?.submissionAttempt,
 			startedAt: options?.startedAt,
 			timeoutAt: options?.timeoutAt,
+			model: input.payload.model,
+			thinkingLevel: input.payload.thinkingLevel,
+			cacheRetention: input.payload.cacheRetention,
 			signal
 		});
 	}
@@ -2787,8 +2789,9 @@ var Session = class {
 	async runPersistedContextInput(options) {
 		return this.withCallOverrides({
 			tools: [],
-			model: void 0,
-			thinkingLevel: void 0
+			model: options.model,
+			thinkingLevel: options.thinkingLevel,
+			cacheRetention: options.cacheRetention
 		}, async ({ resolvedModel }) => {
 			this.activeSubmissionId = options.submissionAttempt?.submissionId;
 			this.activeSubmissionAttemptId = options.submissionAttempt?.attemptId;
diff --git a/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs b/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs
index 7aad15d8afe459fc75d990bde7bacc0992a62529..1c8b3a60f17f814ce29f4bf43fd828fcaa97fb4d 100644
--- a/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs
+++ b/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs
@@ -10,7 +10,10 @@ const DirectAgentImageSchema = v.object({
 });
 const DirectAgentPayloadSchema = v.object({
 	message: v.string(),
-	images: v.optional(v.array(DirectAgentImageSchema))
+	images: v.optional(v.array(DirectAgentImageSchema)),
+	model: v.optional(v.pipe(v.string(), v.maxLength(512))),
+	thinkingLevel: v.optional(v.pipe(v.string(), v.maxLength(32))),
+	cacheRetention: v.optional(v.picklist(["none", "short", "long"]))
 });
 const WorkflowRouteParamSchema = v.object({ name: v.string() });
 /** Shared `?wait` query contract for agent and workflow invocation routes. */
diff --git a/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts b/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts
index e41ab79c03df8f1af64e73af81b76e545d17fa69..1b1a3c4142ba10cfcbe9b75eaebe6dcde7e981b0 100644
--- a/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts
+++ b/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts
@@ -83,6 +83,12 @@ type DirectAgentImage = PromptImage & {
 interface DirectAgentPayload {
   message: string;
   images?: DirectAgentImage[];
+  /** Flary may pin one admitted model for this durable submission. */
+  model?: string;
+  /** Flary may pin one admitted reasoning level for this durable submission. */
+  thinkingLevel?: string;
+  /** Flary may pin one provider-native cache policy for this submission. */
+  cacheRetention?: "none" | "short" | "long";
 }
 /** Context passed to a {@link defineAgent} initializer. */
 interface AgentInitializerContext<TEnv = Record<string, any>> {
