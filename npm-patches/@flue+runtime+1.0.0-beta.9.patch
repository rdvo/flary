diff --git a/node_modules/@flue/runtime/dist/conversation-projections-XMug3C6A.mjs b/node_modules/@flue/runtime/dist/conversation-projections-XMug3C6A.mjs
index d052200..247924a 100644
--- a/node_modules/@flue/runtime/dist/conversation-projections-XMug3C6A.mjs
+++ b/node_modules/@flue/runtime/dist/conversation-projections-XMug3C6A.mjs
@@ -99,7 +99,8 @@ const AgentProfileSchema = v.strictObject({
 	subagents: v.optional(v.array(v.unknown())),
 	thinkingLevel: v.optional(v.string()),
 	compaction: v.optional(v.union([v.literal(false), v.looseObject({})])),
-	durability: v.optional(v.looseObject({}))
+	durability: v.optional(v.looseObject({})),
+	approvalContinuation: v.optional(v.unknown())
 }, (issue) => issue.expected === "never" ? `received unknown agent profile field ${issue.received}` : issue.message);
 const AGENT_RUNTIME_FIELDS = new Set([
 	...Object.keys(AgentProfileSchema.entries),
@@ -163,7 +164,8 @@ function resolveAgentProfile(options) {
 		subagents: mergeArrays(profile?.subagents, options?.subagents),
 		thinkingLevel: hasOwn(options, "thinkingLevel") ? options?.thinkingLevel : profile?.thinkingLevel,
 		compaction: hasOwn(options, "compaction") ? options?.compaction : profile?.compaction,
-		durability: hasOwn(options, "durability") ? options?.durability : profile?.durability
+		durability: hasOwn(options, "durability") ? options?.durability : profile?.durability,
+		approvalContinuation: hasOwn(options, "approvalContinuation") ? options?.approvalContinuation : profile?.approvalContinuation
 	};
 }
 function extendAgentProfile(profile, extensions) {
@@ -1528,7 +1530,15 @@ function getActiveConversationPath(conversation) {
 	return path.reverse();
 }
 function buildConversationContextEntries(conversation, options = {}) {
-	const path = getActiveConversationPath(conversation);
+	const canonicalPath = getActiveConversationPath(conversation);
+	const rollbackIndex = canonicalPath.findLastIndex((entry) => entry.type === "message" && entry.message.role === "signal" && entry.message.type === "flary_rollback");
+	const rollback = rollbackIndex >= 0 ? canonicalPath[rollbackIndex] : void 0;
+	const targetEntryId = rollback?.type === "message" && rollback.message.role === "signal" ? rollback.message.attributes?.targetEntryId : void 0;
+	const targetIndex = typeof targetEntryId === "string" ? canonicalPath.findIndex((entry) => entry.id === targetEntryId) : -1;
+	const path = rollbackIndex >= 0 && targetIndex >= 0 && targetIndex < rollbackIndex ? [
+		...canonicalPath.slice(0, rollback?.type === "message" && rollback.message.role === "signal" && rollback.message.attributes?.excludeTarget === true ? targetIndex : targetIndex + 1),
+		...canonicalPath.slice(rollbackIndex)
+	] : canonicalPath;
 	const latestCompactionIndex = path.findLastIndex((entry) => entry.type === "compaction");
 	if (latestCompactionIndex === -1) return pathToContextEntries(path, options);
 	const compaction = path[latestCompactionIndex];
diff --git a/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs b/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs
index 9403255..94471b3 100644
--- a/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs
+++ b/node_modules/@flue/runtime/dist/conversation-stream-store-Bitz7UoW.mjs
@@ -15,6 +15,16 @@ const DURABILITY_DEFAULT_MAX_ATTEMPTS = 10;
 const DURABILITY_DEFAULT_TIMEOUT_MS = 36e5;
 /** Default lease duration for submission ownership in milliseconds (30 seconds). */
 const LEASE_DURATION_MS = 3e4;
+class ApprovalWaitingError extends Error {
+	code = "approval_pending";
+	constructor() {
+		super("The agent is waiting for an approval decision.");
+		this.name = "ApprovalWaitingError";
+	}
+}
+function isApprovalWaitingError(error) {
+	return error instanceof ApprovalWaitingError || Boolean(error && typeof error === "object" && error.code === "approval_pending");
+}
 //#endregion
 //#region src/context.ts
 /**
@@ -565,6 +575,7 @@ var Session = class {
 	activeTimeoutAt;
 	activeSubmissionId;
 	activeSubmissionAttemptId;
+	activeCacheRetention;
 	conversationWriter;
 	attachmentStore;
 	canonicalAssistant;
@@ -577,7 +588,11 @@ var Session = class {
 		if (this.activeTurnId === void 0) this.activeTurnId = generateTurnId();
 		const turnId = this.activeTurnId;
 		const operationId = this.activeOperationId ?? generateOperationId();
-		this.emitTurnRequest(turnId, "agent", model, context, options);
+		const streamOptions = {
+			...options,
+			...this.activeCacheRetention ? { cacheRetention: this.activeCacheRetention } : {}
+		};
+		this.emitTurnRequest(turnId, "agent", model, context, streamOptions);
 		const operation = {
 			type: "model",
 			turnId
@@ -586,7 +601,7 @@ var Session = class {
 			operationId,
 			turnId
 		});
-		return interceptExecution(operation, executionContext, async () => wrapProviderStream(streamSimple(model, context, options), operation, executionContext));
+		return interceptExecution(operation, executionContext, async () => wrapProviderStream(streamSimple(model, context, streamOptions), operation, executionContext));
 	};
 	canonicalEnvelope(type, id = generateConversationRecordId()) {
 		return {
@@ -1159,8 +1174,26 @@ var Session = class {
 		if (!following) return;
 		const partial = findTrailingPartialToolBatch(following.flatMap((entry) => entry.type === "message" ? [entry] : []));
 		if (!partial) return;
+		if (await this.hasWaitingApproval(conversation, partial, signal)) throw new ApprovalWaitingError();
 		const resolvedTaskOutcomes = await this.resumeUnresolvedTaskCalls(conversation, partial, signal);
-		await this.appendRepairedToolResultBatch(partial.entryId, partial.toolCalls, conversation, resolvedTaskOutcomes);
+		const resolvedApprovalOutcomes = await this.resumeUnresolvedApprovalCalls(conversation, partial, signal);
+		const resolved = new Map([...resolvedTaskOutcomes, ...resolvedApprovalOutcomes]);
+		await this.appendRepairedToolResultBatch(partial.entryId, partial.toolCalls, conversation, resolved);
+	}
+	async hasWaitingApproval(conversation, partial, signal) {
+		if (!this.config.approvalContinuation) return false;
+		for (const toolCall of partial.toolCalls) {
+			if (conversation.toolOutcomes.get(toolOutcomeKey(partial.entryId, toolCall.id))) continue;
+			const argumentsValue = partial.assistant.content.find((block) => block.type === "toolCall" && block.id === toolCall.id)?.arguments ?? {};
+			const state = await this.config.approvalContinuation.inspect({
+				toolCallId: toolCall.id,
+				toolName: toolCall.name,
+				arguments: argumentsValue,
+				signal
+			});
+			if (state === "waiting") return true;
+		}
+		return false;
 	}
 	/**
 	* Resume each unresolved model-invoked `task` call in a trailing partial
@@ -1183,6 +1216,45 @@ var Session = class {
 		}
 		return resolved;
 	}
+	async resumeUnresolvedApprovalCalls(conversation, partial, signal) {
+		const resolved = /* @__PURE__ */ new Map();
+		if (!this.config.approvalContinuation) return resolved;
+		for (const toolCall of partial.toolCalls) {
+			if (conversation.toolOutcomes.get(toolOutcomeKey(partial.entryId, toolCall.id))) continue;
+			const argumentsValue = partial.assistant.content.find((block) => block.type === "toolCall" && block.id === toolCall.id)?.arguments ?? {};
+			const state = await this.config.approvalContinuation.inspect({
+				toolCallId: toolCall.id,
+				toolName: toolCall.name,
+				arguments: argumentsValue,
+				signal
+			});
+			if (state === "waiting") throw new ApprovalWaitingError();
+			if (state !== "ready") continue;
+			const result = await this.config.approvalContinuation.resume({
+				toolCallId: toolCall.id,
+				toolName: toolCall.name,
+				arguments: argumentsValue,
+				signal
+			});
+			resolved.set(toolCall.id, this.approvalResumeOutcomeRecord(partial.entryId, toolCall, result));
+		}
+		return resolved;
+	}
+	approvalResumeOutcomeRecord(assistantEntryId, toolCall, result) {
+		const key = `${encodeCanonicalId(assistantEntryId)}_${encodeCanonicalId(toolCall.id)}`;
+		return {
+			...this.canonicalEnvelope("tool_outcome", `record_tool_approval_outcome_${key}`),
+			type: "tool_outcome",
+			assistantMessageId: assistantEntryId,
+			toolCallId: toolCall.id,
+			toolName: toolCall.name,
+			isError: Boolean(result.isError),
+			content: [{
+				type: "text",
+				text: result.content || "{}"
+			}]
+		};
+	}
 	/** Reattach to one in-flight child, resume it to completion, and build the
 	*  parent's real `tool_outcome` for the originating `task` call. */
 	async resumeChildTaskCall(assistantEntryId, assistant, toolCallId, ref, signal) {
@@ -1881,9 +1953,11 @@ var Session = class {
 		const previousTools = this.agentLoop.state.tools;
 		const previousModel = this.agentLoop.state.model;
 		const previousThinkingLevel = this.agentLoop.state.thinkingLevel;
+		const previousCacheRetention = this.activeCacheRetention;
 		const resolvedModel = this.resolveModelForCall(options.model);
 		this.agentLoop.state.model = resolvedModel;
 		this.agentLoop.state.thinkingLevel = this.resolveThinkingLevelForCall(options.thinkingLevel);
+		this.activeCacheRetention = options.cacheRetention;
 		const builtinToolGroups = this.createBuiltinToolGroups(this.env, options.tools, options.model, options.thinkingLevel, options.activePackagedSkills);
 		this.agentLoop.state.tools = this.assembleModelTools(builtinToolGroups, [...this.agentTools, ...options.tools], options.extraTools ?? []);
 		try {
@@ -1892,6 +1966,7 @@ var Session = class {
 			this.agentLoop.state.tools = previousTools;
 			this.agentLoop.state.model = previousModel;
 			this.agentLoop.state.thinkingLevel = previousThinkingLevel;
+			this.activeCacheRetention = previousCacheRetention;
 		}
 	}
 	resolveDeclaredSubagent(name) {
@@ -2619,6 +2694,14 @@ var Session = class {
 	canonicalInputEntryId(input) {
 		return input.kind === "dispatch" ? submissionEntryId("dispatch", input.dispatchId) : submissionEntryId("direct", input.submissionId);
 	}
+	async inspectPendingApproval(input, signal) {
+		if (!this.config.approvalContinuation) return false;
+		const conversation = await this.requireConversation();
+		const following = getActiveConversationPathSince(conversation, this.canonicalInputEntryId(input));
+		if (!following) return false;
+		const partial = findTrailingPartialToolBatch(following.flatMap((entry) => entry.type === "message" ? [entry] : []));
+		return partial ? this.hasWaitingApproval(conversation, partial, signal) : false;
+	}
 	inspectCanonicalState(state) {
 		switch (state.kind) {
 			case "absent": return "absent";
@@ -2775,6 +2858,9 @@ var Session = class {
 			submissionAttempt: options?.submissionAttempt,
 			startedAt: options?.startedAt,
 			timeoutAt: options?.timeoutAt,
+			model: input.payload.model,
+			thinkingLevel: input.payload.thinkingLevel,
+			cacheRetention: input.payload.cacheRetention,
 			signal
 		});
 	}
@@ -2787,8 +2873,9 @@ var Session = class {
 	async runPersistedContextInput(options) {
 		return this.withCallOverrides({
 			tools: [],
-			model: void 0,
-			thinkingLevel: void 0
+			model: options.model,
+			thinkingLevel: options.thinkingLevel,
+			cacheRetention: options.cacheRetention
 		}, async ({ resolvedModel }) => {
 			this.activeSubmissionId = options.submissionAttempt?.submissionId;
 			this.activeSubmissionAttemptId = options.submissionAttempt?.attemptId;
@@ -3025,10 +3112,10 @@ function createDirectAgentSubmissionInput(options) {
 	return {
 		kind: "direct",
-		submissionId: crypto.randomUUID(),
+		submissionId: options.payload.idempotencyKey ?? crypto.randomUUID(),
 		agent: options.agent,
 		id: options.id,
 		payload: options.payload,
 		acceptedAt: (/* @__PURE__ */ new Date()).toISOString(),
 		...options.traceCarrier ? { traceCarrier: options.traceCarrier } : {}
 	};
 }
@@ -3126,7 +3213,8 @@ async function reconcileInterruptedSubmission(submissions, submission, agent, cr
 		const state = await s.inspectSubmissionInput(input);
 		return {
 			state,
-			result: state === "completed" ? await s.reconstructSubmissionResult(input) : void 0
+			result: state === "completed" ? await s.reconstructSubmissionResult(input) : void 0,
+			waiting: state === "tool_use_unresolved" ? await s.inspectPendingApproval(input) : false
 		};
 	})(ctx);
 	const state = inspected.state;
@@ -3138,6 +3226,7 @@ async function reconcileInterruptedSubmission(submissions, submission, agent, cr
 			result: inspected.result
 		};
 	}
+	if (inspected.waiting && submission.abortRequestedAt === void 0) return { disposition: "waiting" };
 	if (submission.abortRequestedAt !== void 0) {
 		const abortCtx = createContext(dispatchId);
 		if (submission.kind === "direct") abortCtx.setSubmissionId?.(submission.submissionId);
@@ -3296,6 +3385,7 @@ async function processSubmission(opts) {
 			}, execute);
 			result = await run();
 		} catch (error) {
+			if (isApprovalWaitingError(error)) return;
 			if (opts.isShutdownAbort?.(error)) {
 				if (submission.kind === "direct") observers.fail(submission.submissionId, error);
 				throw error;
diff --git a/node_modules/@flue/runtime/dist/internal.d.mts b/node_modules/@flue/runtime/dist/internal.d.mts
index c785e22..714ff51 100644
--- a/node_modules/@flue/runtime/dist/internal.d.mts
+++ b/node_modules/@flue/runtime/dist/internal.d.mts
@@ -84,6 +84,14 @@ interface CloudflareAgentRuntime {
   attach(instance: CloudflareAgentInstance, prepared: CloudflareAgentPreparedCoordinator): void;
   onStart(instance: CloudflareAgentInstance, inherited: () => Promise<unknown> | unknown): Promise<void>;
   wakeSubmissions(instance: CloudflareAgentInstance): Promise<void>;
+  compact(instance: CloudflareAgentInstance): Promise<void>;
+  rollback(instance: CloudflareAgentInstance, turnId: string, reason?: string, excludeTarget?: boolean): Promise<{
+    turnId: string;
+    targetEntryId: string;
+    markerId: string;
+  }>;
+  exportCanonical(instance: CloudflareAgentInstance, turnId?: string): Promise<unknown>;
+  importCanonical(instance: CloudflareAgentInstance, archive: unknown, turnId?: string): Promise<unknown>;
   onRequest(instance: CloudflareAgentInstance, request: Request): Promise<Response | null>;
   onFiberRecovered(instance: CloudflareAgentInstance, ctx: CloudflareAgentRecoveredFiberContext, inherited: () => Promise<unknown> | unknown): Promise<unknown>;
 }
diff --git a/node_modules/@flue/runtime/dist/internal.mjs b/node_modules/@flue/runtime/dist/internal.mjs
index 1147b97..03531c4 100644
--- a/node_modules/@flue/runtime/dist/internal.mjs
+++ b/node_modules/@flue/runtime/dist/internal.mjs
@@ -703,7 +703,8 @@ async function initializeRootHarness(agent, config, emitEvent) {
 		model: resolvedModel,
 		thinkingLevel: definition.thinkingLevel ?? config.agentConfig.thinkingLevel,
 		compaction: definition.compaction ?? config.agentConfig.compaction,
-		durability: definition.durability
+		durability: definition.durability,
+		approvalContinuation: definition.approvalContinuation
 	};
 	if (!config.conversationWriter || !config.attachmentStore) throw new Error("[flue] Canonical conversation runtime is not configured.");
 	return new Harness(config.id, "default", agentConfig, env, emitEvent, definition.tools ?? [], toolFactory, config.conversationWriter, config.attachmentStore, definition.actions, config.runId === void 0 ? { instanceId: config.id } : { runId: config.runId });
@@ -793,6 +794,18 @@ function createCloudflareAgentRuntime(options) {
 		wakeSubmissions(instance) {
 			return getCoordinator(instance).wakeSubmissions();
 		},
+		compact(instance) {
+			return getCoordinator(instance).compact();
+		},
+		rollback(instance, turnId, reason, excludeTarget) {
+			return getCoordinator(instance).rollback(turnId, reason, excludeTarget);
+		},
+		exportCanonical(instance, turnId) {
+			return getCoordinator(instance).exportCanonical(turnId);
+		},
+		importCanonical(instance, archive, turnId) {
+			return getCoordinator(instance).importCanonical(archive, turnId);
+		},
 		onRequest(instance, request) {
 			return getCoordinator(instance).onRequest(request);
 		},
@@ -840,11 +853,111 @@ var CloudflareAgentCoordinator = class {
 			await this.reconcileSubmissions({ driverAlreadyArmed: true });
 		});
 	}
+	compact() {
+		return this.runWithInstanceContext(async () => {
+			if (await this.submissions.hasUnsettledSubmissions()) throw new Error("[flue] Cannot compact while an agent submission is active.");
+			const agent = this.options.agents.find((record) => record.name === this.agentName)?.definition;
+			if (!agent) throw new Error("[flue] Agent target unavailable during manual compaction.");
+			await this.ensureConversationWriter();
+			const ctx = this.createDurableContext(new Request("https://flue.internal/?flary=compact", { method: "POST" }));
+			const session = await (await ctx.initializeRootHarness(agent)).session("default");
+			await session.compact();
+		});
+	}
+	rollback(turnId, reason, excludeTarget) {
+		return this.runWithInstanceContext(async () => {
+			if (await this.submissions.hasUnsettledSubmissions()) throw new Error("[flue] Cannot roll back while an agent submission is active.");
+			const writer = await this.ensureConversationWriter();
+			const state = await writer.loadReducedState();
+			const conversation = [...state.conversations.values()].find((candidate) => candidate.kind === "root" && candidate.harness === "default" && candidate.session === "default") ?? [...state.conversations.values()].find((candidate) => candidate.kind === "root");
+			if (!conversation) throw new Error("[flue] The root conversation does not exist.");
+			let targetEntryId = conversation.entries.has(turnId) ? turnId : void 0;
+			if (!targetEntryId) {
+				for (const record of state.recordsById.values()) {
+					if (record.conversationId !== conversation.conversationId || record.turnId !== turnId) continue;
+					if (typeof record.messageId === "string" && conversation.entries.has(record.messageId)) targetEntryId = record.messageId;
+					if (typeof record.entryId === "string" && conversation.entries.has(record.entryId)) targetEntryId = record.entryId;
+				}
+			}
+			if (!targetEntryId) throw new Error(`[flue] Rollback target "${turnId}" was not found in the canonical conversation.`);
+			const parentId = conversation.activeLeafId;
+			const messageId = `entry_flary_rollback_${crypto.randomUUID()}`;
+			await writer.append([{
+				v: 1,
+				id: `record_flary_rollback_${crypto.randomUUID()}`,
+				type: "signal",
+				conversationId: conversation.conversationId,
+				harness: conversation.harness,
+				session: conversation.session,
+				timestamp: (/* @__PURE__ */ new Date()).toISOString(),
+				messageId,
+				parentId,
+				signalType: "flary_rollback",
+				tagName: "rollback",
+				content: reason || `Rolled back to ${turnId}.`,
+				attributes: { targetEntryId, turnId, excludeTarget: excludeTarget === true }
+			}]);
+			return { turnId, targetEntryId, markerId: messageId };
+		});
+	}
+	exportCanonical(turnId) {
+		return this.runWithInstanceContext(async () => {
+			if (await this.submissions.hasUnsettledSubmissions()) throw new Error("[flue] Cannot export while an agent submission is active.");
+			const writer = await this.ensureConversationWriter();
+			await writer.flush();
+			const batches = [];
+			let offset = "-1";
+			for (;;) {
+				const page = await writer.store.read(writer.path, { offset, limit: 1e3 });
+				batches.push(...page.batches.map((batch) => batch.records));
+				offset = page.nextOffset;
+				if (page.upToDate) break;
+			}
+			return { format: "flue-canonical", version: 1, batches, throughTurnId: turnId };
+		});
+	}
+	importCanonical(archive, turnId) {
+		return this.runWithInstanceContext(async () => {
+			if (await this.submissions.hasUnsettledSubmissions()) throw new Error("[flue] Cannot import while an agent submission is active.");
+			if (!archive || archive.format !== "flue-canonical" || archive.version !== 1 || !Array.isArray(archive.batches)) throw new Error("[flue] The canonical archive is invalid.");
+			const writer = await this.ensureConversationWriter();
+			const meta = await writer.store.getMeta(writer.path);
+			if (meta && meta.nextOffset !== "-1") throw new Error("[flue] Canonical import needs an empty agent thread.");
+			for (const batch of archive.batches) {
+				if (!Array.isArray(batch)) throw new Error("[flue] The canonical archive batch is invalid.");
+				const records = batch.flatMap((record) => {
+					if (!record || typeof record !== "object" || record.type === "submission_settled") return [];
+					const { submissionId, attemptId, ...portable } = record;
+					return [portable];
+				});
+				if (records.length > 0) await writer.append(records);
+			}
+			if (turnId) {
+				const state = await writer.loadReducedState();
+				const conversation = [...state.conversations.values()].find((candidate) => candidate.kind === "root" && candidate.harness === "default" && candidate.session === "default") ?? [...state.conversations.values()].find((candidate) => candidate.kind === "root");
+				if (!conversation) throw new Error("[flue] The imported root conversation does not exist.");
+				let targetEntryId = conversation.entries.has(turnId) ? turnId : void 0;
+				if (!targetEntryId) for (const record of state.recordsById.values()) {
+					if (record.conversationId !== conversation.conversationId || record.turnId !== turnId) continue;
+					if (typeof record.messageId === "string" && conversation.entries.has(record.messageId)) targetEntryId = record.messageId;
+					if (typeof record.entryId === "string" && conversation.entries.has(record.entryId)) targetEntryId = record.entryId;
+				}
+				if (!targetEntryId) throw new Error(`[flue] Fork target "${turnId}" was not found in the imported conversation.`);
+				const messageId = `entry_flary_rollback_${crypto.randomUUID()}`;
+				await writer.append([{ v: 1, id: `record_flary_rollback_${crypto.randomUUID()}`, type: "signal", conversationId: conversation.conversationId, harness: conversation.harness, session: conversation.session, timestamp: new Date().toISOString(), messageId, parentId: conversation.activeLeafId, signalType: "flary_rollback", tagName: "rollback", content: `Forked through ${turnId}.`, attributes: { targetEntryId, turnId } }]);
+			}
+			return { imported: true, batches: archive.batches.length, throughTurnId: turnId };
+		});
+	}
 	onRequest(request) {
 		return this.runWithInstanceContext(() => this.routeRequest(request));
 	}
 	async routeRequest(request) {
 		if (isInternalDispatchRequest(request)) return this.admitDispatch(request);
+		if (new URL(request.url).searchParams.get("flary") === "wake" && request.method === "GET") {
+			await this.wakeSubmissions();
+			return Response.json({ ok: true });
+		}
 		if (isAbortRequest(request, this.agentName, this.instance.name)) {
 			const aborted = await this.abortInstance();
 			return Response.json({ aborted });
diff --git a/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs b/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs
index 7aad15d..c3f38a8 100644
--- a/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs
+++ b/node_modules/@flue/runtime/dist/run-store-CYeXjR-d.mjs
@@ -10,7 +10,11 @@ const DirectAgentImageSchema = v.object({
 });
 const DirectAgentPayloadSchema = v.object({
 	message: v.string(),
-	images: v.optional(v.array(DirectAgentImageSchema))
+	images: v.optional(v.array(DirectAgentImageSchema)),
+	idempotencyKey: v.optional(v.pipe(v.string(), v.maxLength(512))),
+	model: v.optional(v.pipe(v.string(), v.maxLength(512))),
+	thinkingLevel: v.optional(v.pipe(v.string(), v.maxLength(32))),
+	cacheRetention: v.optional(v.picklist(["none", "short", "long"]))
 });
 const WorkflowRouteParamSchema = v.object({ name: v.string() });
 /** Shared `?wait` query contract for agent and workflow invocation routes. */
diff --git a/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts b/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts
index e41ab79..94f8abb 100644
--- a/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts
+++ b/node_modules/@flue/runtime/dist/types-USSZhfC6.d.mts
@@ -83,6 +83,14 @@ type DirectAgentImage = PromptImage & {
 interface DirectAgentPayload {
   message: string;
   images?: DirectAgentImage[];
+  /** Stable Flary admission key used to make retries idempotent. */
+  idempotencyKey?: string;
+  /** Flary may pin one admitted model for this durable submission. */
+  model?: string;
+  /** Flary may pin one admitted reasoning level for this durable submission. */
+  thinkingLevel?: string;
+  /** Flary may pin one provider-native cache policy for this submission. */
+  cacheRetention?: "none" | "short" | "long";
 }
 /** Context passed to a {@link defineAgent} initializer. */
 interface AgentInitializerContext<TEnv = Record<string, any>> {
@@ -287,6 +293,25 @@ interface DurabilityConfig {
    */
   timeoutMs?: number;
 }
+type ApprovalRecoveryState = 'none' | 'waiting' | 'ready';
+interface ApprovalContinuation {
+  inspect(input: {
+    toolCallId: string;
+    toolName: string;
+    arguments: Record<string, unknown>;
+    signal?: AbortSignal;
+  }): ApprovalRecoveryState | Promise<ApprovalRecoveryState>;
+  resume(input: {
+    toolCallId: string;
+    toolName: string;
+    arguments: Record<string, unknown>;
+    signal?: AbortSignal;
+  }): Promise<{
+    content: string;
+    isError?: boolean;
+    output?: unknown;
+  }>;
+}
 interface AgentConfig {
   /** Discovered at runtime from AGENTS.md + .agents/skills/ in the session's cwd. */
   systemPrompt: string;
@@ -316,6 +341,8 @@ interface AgentConfig {
   compaction?: false | CompactionConfig;
   /** Durability settings resolved from the agent profile. */
   durability?: DurabilityConfig;
+  /** Durable approval pause and resume adapter. */
+  approvalContinuation?: ApprovalContinuation;
 }
 /** Reusable agent behavior accepted by {@link defineAgentProfile}. */
 interface AgentProfile {
@@ -350,6 +377,8 @@ interface AgentProfile {
    * independent durability configuration of its own.
    */
   durability?: DurabilityConfig;
+  /** Durable approval pause and resume adapter. */
+  approvalContinuation?: ApprovalContinuation;
 }
 /** Configuration returned by a {@link defineAgent} initializer. */
 interface AgentRuntimeConfig {
@@ -381,6 +410,8 @@ interface AgentRuntimeConfig {
    * recovery attempt limits and submission timeouts.
    */
   durability?: DurabilityConfig;
+  /** Durable approval pause and resume adapter. */
+  approvalContinuation?: ApprovalContinuation;
   /** Working directory inside the initialized sandbox. */
   cwd?: string;
   /** Sandbox factory used to construct the initialized environment. */
